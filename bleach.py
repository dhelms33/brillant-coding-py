class Bleach:
    def __init__(self, name, Ichigo, Rukia):
        self.name = name
        self.main_chars = []

    def is_number_one(self, self.name):
        if self.name.lower() == "ichigo":
            return True
        else:
            return False
    
    def num_main_char(self.main_chars):
        count_char = 0
        for char in self.main_chars:
            count_char+=1
        return count_char
    
    def calc_main_char(self.main_chars):
        for char in range(len(self.main_chars)):
            total += num_main_char(char)
        return total
    